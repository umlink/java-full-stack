# 阶段七 · 小点 4：项目 3 —— AI 应用平台（前沿方向，可选）

> 所属：阶段七 全栈融合与项目实战
> 定位：把 Java 后端能力延伸到 AI 工程化——**AI 功能不悬空，长在企业架构上**：缓存 / MQ / 鉴权 / 观测这些传统能力，与 LLM 调用的稳定性 / 成本治理组合，才是这个项目的含金量（也是你 Python Agent 学习路线在 Java 侧的镜像练习）。
>
> ⚠️ **可选项**：前两个项目做透后有余力再做；**跳过硬技术不影响主线**。做完项目一二已是高级后端能力，AI 平台是差异化加分项——别为了它拖垮主线进度。

## 快速入门

> 本节为「项目三速览」：先认识这个项目做什么、技术栈怎么选、核心链路是什么；「RAG、Prompt 管理细节」留在正文提高部分。

### 项目三核心关键词速查

| 关键词 | 一句话大白话 | 例子 |
|-|-|-|
| AI 应用平台 | 接 LLM 的业务系统 | 智能问答、知识库 |
| Spring AI | Spring 官方的 AI 框架 | 与 Boot 集成 |
| LangChain4j | Java 侧 LLM 编排库 | 灵活 |
| RAG | 检索增强：先查资料再问模型 | 知识库问答 |
| 向量库 | 存「语义向量」的库 | PGVector/Milvus |
| 流式对话 | token 逐段返回 | SSE 打字机 |
| Prompt | 给模型的指令 | 版本管理 |
| 降级链 | 大模型挂了用小模型 | 重试/降级/成本 |

### 项目三做什么

- **定位**：把 Java 后端能力延伸到 AI 工程化——**AI 功能不悬空，长在企业架构上**：缓存 / MQ / 鉴权 / 观测与传统能力，与 LLM 调用的稳定性 / 成本治理组合，才是含金量。
- **可选项**：前两个项目做透后余力再做；**跳过硬技术不影响主线**。
- **技术栈**：Spring AI 或 LangChain4j（二选一，前者和 Boot 集成好）+ PGVector（默认，复用已有 PG）+ React + SSE。
- **你要带走的一句话**：这个项目的含金量不在「调 LLM」，而在**AI 工程化**——重试/降级/成本控制，把 LLM 当「脆弱的外部依赖」来治理。

### 核心链路速览（正文展开更细）

```text
RAG 知识库:  文档解析 → 切分 → 向量化 → 存向量库(离线索引)
            用户提问 → 问题向量化 → 检索(top-k) → 拼Prompt → LLM生成 → 回答
AI 工程化:   重试(指数退避) + 降级(大→小→检索摘要) + 成本(打点+汇总)
```

> 说明（逐行解释）：
> - **RAG**：把文档切分、向量化、存进向量库；用户提问时先检索相关知识再让模型回答——防止「模型不知道还在编」。
> - **向量库选型**：默认用 **PGVector**（复用已有 PostgreSQL，零额外部署）；量大再到百万级才考虑 Milvus——别一开始就上重组件。
> - **流式对话**：用 SSE 逐 token 推送，前端打字机效果；断线重连、取消生成要处理。
> - **AI 工程化**：把 LLM 当脆弱外部依赖——重试（指数退避）、降级（大模型→小模型→纯检索摘要）、成本（token 打点 + 汇总）。

### 关键概念说明

| 概念 | 干什么 | 最易踩的坑 |
|-|-|-|
| RAG | 检索增强 | 切分质量 > 模型大小；检索置信低要拒答 |
| 向量库 | 存语义向量 | 默认 PGVector，别急着上 Milvus |
| 流式对话 | SSE 逐 token | 逐 token 的 idle timeout 比总超时适合 |
| Prompt 管理 | 版本化 + 评测 | 评测集要按场景×难度设计 |
| 降级链 | 大→小→检索摘要 | 只重试幂等/只读 |
| 成本控制 | token 打点 | 先打点+日汇总，配额限流暂不做 |

### 常用约定 / 命名提示

- **这个项目是可选**：前两个做透后再来；跳过硬技术不影响主线，别为了它拖垮进度。
- **AI 工程化是核心**：重试/降级/成本/审计/Prompt 注入检测——把 AI 当依赖治理，而不是裸调 API。
- **向量库别过度**：默认 PGVector（复用已有 PG）；Rerank 是进阶，检索精度不够才加，不是起点。

## 精简大纲

1. 技术栈选型：Spring AI / LangChain4j 二选一
2. 核心功能 1：RAG 知识库全链路
3. 核心功能 2：流式对话（SSE）与前端打字机
4. 核心功能 3：Prompt 版本管理
5. 重点：AI 工程化（重试/降级/成本控制）与传统架构组合

## 学习内容详情

### 1. 技术栈选型

- **Spring AI 或 LangChain4j（二选一）**：
  - **Spring AI**：Spring 官方，与 Boot 4 深度集成（自动配置 / starter / ChatClient 走 Spring 生态心智）——**生态集成好**，主栈是全家桶就选它。
  - **LangChain4j**：社区驱动，抽象贴近 LangChain、编排更灵活——**灵活性高**，需要自定义链路时评估。
- **向量库**：**默认 PGVector**——复用已有 PostgreSQL，零额外部署，百万级向量够用；**仅当向量规模到百万级以上 / 检索 P99 不达标时才考虑独立 Milvus**。别一上来就选重组件（选型沿用阶段三「先复用后新增」原则）。
- 前端：React（流式 UI 主场，SSE 消费你有现成肌肉记忆）。

```xml
<!-- Spring AI 起步: 一个 starter + 一个模型配置 -->
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-openai-spring-boot-starter</artifactId>   <!-- OpenAI 兼容协议, 换国产模型改 base-url -->
</dependency>
```

```yaml
spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}          # 密钥永远从环境变量(阶段二第 2 讲红线)
      base-url: https://ark.example.com   # 换服务商只改这行
      chat.options.model: doubao-pro-128k
      chat.options.temperature: 0.2       # 工具调用/检索问答场景低温(阶段一 Python 版同款经验)
```

### 2. RAG 知识库全链路

```text
文档解析 → 切分(chunking) → 向量化(embedding) → 存向量库  ← 离线索引链路
用户提问 → 问题向量化 → 相似检索(top-k) → 拼 Prompt(带引用上下文) → LLM 生成 → 回答   ← 在线问答链路
```

```java
@Service
public class RagService {
    private final VectorStore vectorStore;        // Spring AI 抽象, Milvus/PGVector 换实现不换代码
    private final ChatClient chat;

    // ① 离线索引: 切分质量 > 模型大小 —— chunk 400~800 token + 10~15% overlap 起步
    public void ingest(List<Document> docs) {
        var chunks = new TokenTextSplitter(600, 80).apply(docs);   // overlap 保句子上下文
        vectorStore.add(chunks);                                   // 内部批量向量化, 注意批大小与超时
    }

    // ② 在线检索增强: 先查上下文再问模型
    public String answer(String question) {
        List<Document> ctx = vectorStore.similaritySearch(
            SearchRequest.query(question).withTopK(5)
                .withSimilarityThreshold(0.75));                   // 阈值挡"看着像其实无关"
        return chat.prompt()
            .system(s -> s.text("""
                仅依据以下资料回答, 没有依据时直说"资料中未找到"; 引用处标 [文档标题]。
                资料: {ctx}""").param("ctx", render(ctx)))
            .user(question)
            .call().content();
    }
}
```

- **Rerank 调优位（进阶扩展）**：top-5 粗召 → rerank 模型精排取 top-3——**检索质量不够时才加的优化，不是起点**。先优化召回与切分，最后才换大模型（性价比直觉同阶段六「自上而下优化」）；主链路只保「文档解析 → 向量检索 → LLM 生成」。
- 空召回也要生成回答还是拒答？产品决策：**检索置信低时拒答 + 引导人工**——「不许模型补齐不存在的数据」是跨语言的铁律。相似度阈值 0.75 不是拍脑袋定的——用小评测集扫 0.6-0.9 区间，选「召回全 + 噪音少」的平衡点。

### 3. 流式对话（SSE）

```java
// WebFlux 的合理主场(阶段二第 4 讲判断): token 逐段推送, 前端打字机
@GetMapping(value = "/chat/stream", produces = MediaType.TEXT_EVENT_STREAM_VALUE)
public Flux<String> chatStream(@RequestParam String q) {
    return chat.prompt().user(q).stream().content()     // Spring AI stream(): Flux token 流
        .timeout(Duration.ofSeconds(60))                // 60s 没新 token = 假死, 断开给前端
        .onErrorResume(e -> Flux.just("[系统繁忙, 请重试]"));   // 半句也要优雅收尾
}
// 前端: EventSource / fetch 流读取逐段渲染; 断线重连带 last-token 标记防重复
```

- 流式链路的可观测：**每个对话一个 trace_id**（阶段二 MDC + 阶段五 Tracing），LLM 调用的 TTFT（首 token 时间）与总耗时单独打点——「慢不慢」对 AI 应用是核心体验指标。
- 边界处理三件套：客户端断连（`doOnCancel` 里 stop 模型生成省钱）、半句超时（上面的 timeout）、内容审核命中（推「敏感词」帧而非 error）。

### 4. Prompt 版本管理

```java
// Prompt 即代码: 版本化 + 评测集驱动回归, 改 Prompt 像改代码一样有测试
// prompts/faq_v3.st (Git 管理, 变更走 MR)
public String answerV3(String q, List<Document> ctx) {
    return promptRegistry.load("faq", "v3")        // 按 key+version 取模板(热更不发版可选配置中心)
        .render(Map.of("ctx", render(ctx), "q", q));
}

// 评测集: 50 条"问题 + 期望要点" —— 换 Prompt/换模型/调阈值 都要全量跑分, 回归门禁进 CI
@Test void promptRegression() {
    var scores = evalSet.load("faq-eval").run(this::answer);   // 评分: 包含关键要点数 / 引用正确性
    assertThat(scores.recallOfKeyPoints()).isGreaterThan(0.9); // 比上一版退化就挡合并
}
```

- **评测集别靠感觉拍**：按「场景 × 难度」矩阵打底，让 50 条有明确结构——常见 FAQ（简单，20 条）/ 多跳推理（中等，15 条）/ 对抗样本·注入·诱导（困难，15 条）。先这样起步，再随 Bad-case 回流逐步扩到 200 条（对应思考题 1）。

- A/B：新旧 Prompt 按流量比例并行，指标（拒答率 / 人工转接率 / 用户点踩）说话——与特性开关同构（阶段五第 3 讲）。

### 5. AI 工程化：把 LLM 当「脆弱的外部依赖」治理

```java
// 重试/降级/成本三件事, 全是后端传统手艺的新场景:
@Service
public class LlmClient {
    @Retryable(retryFor = TimeoutException.class, maxAttempts = 2,        // 只重试幂等/只读
               backoff = @Backoff(delay = 2000, multiplier = 2))         // 指数退避, LLM 429 常见
    @CircuitBreaker(name = "llm", fallbackMethod = "degrade")
    public ChatResponse call(ChatRequest req) { return rawCall(req); }

    ChatResponse degrade(ChatRequest req, Throwable t) {
        return chatSmallModel(req);                                       // 降级链: 大模型→小模型→纯检索摘要→拒答兜底
    }
}
```

- **成本控制**：token 用量每次调用打点（usage 元数据）+ 每日汇总即可起步；**「按用户/场景配额限流」先不做**——那要额外做一套计量系统（Redis 计数 + 定时清理 + 用户账单），超出本可选项的必要复杂度。有需要再按阶段六限流器叠加。
- **与中间件的组合清单**：对话历史（Redis TTL + PG 归档）、检索结果缓存（embedding 缓存省向量检索费）、敏感词前置（挡在调模型之前）、审计日志（谁问了什么、模型答了什么）、**Prompt 注入检测**（用户输入里的「忽略以上指令」要当攻击面看，阶段六第 5 讲的新变种）。

### 坑点提醒

- **temperature 当"随机种子"用**：0 不等于可复现（模型侧仍非确定）——评测与回归要跑多次取分布，别拿单次结果换 Prompt。
- **RAG 把整篇文档塞进上下文**：token 成本与「中间遗忘」双输——切分 + 召回 + 精排的目的就是控制上下文规模。
- **流式接口没算超时预算**：网关 60s / 模型 120s 的生成会静默截断——逐 token 的 idle timeout 比总超时更适合流式（上面 60s 的写法）。
- **向量库当数据库用**：metadata 过滤能力弱、更新有延迟——业务真相进 MySQL / PG，向量库是检索加速层（ES 同款定位）。

## 本节自检

- [ ] 能说清 Spring AI 与 LangChain4j 的选型依据，以及为什么前者与 Boot 4 集成更顺
- [ ] 能实现一条完整 RAG 链路，并解释切分策略与相似度阈值对效果的影响
- [ ] 流式对话能处理「断线重连 + 半句恢复 + 取消生成」三个边界
- [ ] AI 功能的成本与稳定性治理（限流 / 配额 / 降级 / 审计）有具体方案而非裸调 API

## 本节配套思考题

1. 评测集 50 条够吗？按「场景 × 难度 × 对抗样本（注入/诱导）」设计一个 200 条的分层评测集框架，每层的通过标准分别是什么？
2. 缓存 RAG 检索结果（question 向量相似当 key）——与阶段三 Cache-Aside 的差异在哪：相似度命中意味着「近似 key」怎么建索引、失效怎么处理（知识库更新时）？
3. 「人工反馈的点踩数据回流成评测集」——设计这条闭环的表结构与流程，它和 Bad-case 治理（你 Python 仓库阶段七）有什么同构之处？

# M1-C-03：认证路由守卫

返回 [实践计划与进度](../README.md)。

## 目标

阻止未登录访客进入 `/users`，并让登录后的会话能安全地返回原目标页。

## 范围

- 为管理路由增加认证守卫，只判断 Token 是否存在。
- 未登录访问 `/users` 时跳转 `/login`，保留受控的返回地址。
- 已登录访问 `/login` 时跳转默认管理页；提供最小退出登录入口并清除会话。

## 学习点

路由守卫、重定向状态、会话恢复与退出清理。

## 验收

1. 无 Token 直接访问或刷新 `/users` 会跳转登录页。
2. 登录后返回原访问目标；退出后无法再进入受保护页。
3. 守卫不以本地角色决定访问结果，后端 403 留给下一张卡片处理。
4. `npm run typecheck`、`npm run lint`、`npm run build` 通过。

## 不做

不请求用户详情，不处理服务端 401，不实现权限菜单。

## 完成记录

日期：2026-09-11

提交：`2d041f1`（feat: 增加管理路由守卫）

测试与页面证据：

- `npm run typecheck`、`npm run lint`、`npm run build` 全部通过（build 仅余 Vite `__dirname` 未来兼容提示，非错误）。
- 实现物：`components/ProtectedRoute.tsx`（仅依据 `getToken()` 判定，未登录把目标路径写入路由 `state.from` 后 `replace` 跳 `/login`，地址不暴露在 URL）、`components/GuestRoute.tsx`（已登录访问 `/login` 弹回 `/users`）、`layouts/AdminLayout.tsx` 侧栏「退出登录」（`clearToken()` + `replace` 回登录页）、`pages/Login/useLoginService.ts` 的 `getReturnPath`（只接受 `/users` 前缀的受控回跳目标）。
- headless Chrome（CDP 驱动真实浏览器，桌面视口 1440×900）10 项检查全部通过：
  - 未登录直接访问 `/users` 跳转 `/login` 且登录页可见；无会话刷新登录页仍停留；无会话重访受保护页再次被拦截。
  - 从被拦截的 `/login` 用真实凭据登录成功后返回原目标 `/users`（SPA 内 `state.from` 回跳），会话令牌写入 `sessionStorage`（长度 148）。
  - 已登录访问 `/login` 被 `GuestRoute` 弹回 `/users`。
  - 点击「退出登录」后回到 `/login`、令牌被清除；退出后再访问 `/users` 无法进入。
- 守卫未在前端判断角色或权限（`ProtectedRoute` 只读 token 存在性），服务端 403 留给 M1-C-04 处理。
- 本地验收环境：8080 被本机其他进程占用，后端以 `SERVER_PORT=8082` 启动、前端 dev server 以 `BACKEND_ORIGIN=http://localhost:8082` 接线（5173-5176 被占，实际端口 5177），注册 `guard03` 普通用户完成登录链路。

# M1-C-04：无权限状态

返回 [实践计划与进度](../README.md)。

## 目标

让已登录但没有后台权限的普通用户在请求被后端拒绝后看到明确、可恢复的无权限状态。

## 范围

- 请求客户端识别 `401/40100` 与 `403/40300` 的不同结果。
- `401` 清除会话并回到登录页；`403` 保留会话并展示无权限页面。
- 无权限页提供返回安全默认页或退出登录的操作。

## 学习点

HTTP 与业务码双层错误契约、前端状态机、后端授权边界。

## 验收

1. 使用普通 `USER` Token 请求 `/users` 时展示无权限状态，不被误判为登录失效。
2. 无效或过期 Token 返回 401 时清除会话并跳转登录页。
3. 页面不展示角色、权限码、堆栈或原始网络异常。
4. `npm run typecheck`、`npm run lint`、`npm run build` 通过。

## 不做

不在前端模拟权限，也不创建角色、权限管理页面。

## 完成记录

日期：2026-09-11

提交：`7347691`（feat: 管理端接入无权限状态）

测试与页面证据：

- `npm run typecheck`、`npm run lint`、`npm run build`、`git diff --check` 全部通过。
- 实现物：
  - `lib/request.ts`：有会话时统一注入 `Authorization: Bearer <token>`；导出 `API_CODE_UNAUTHORIZED(40100)` / `API_CODE_FORBIDDEN(40300)` 业务码常量。携带令牌的请求收到 `40100` 时清理会话并整页跳转 `/login`；**不带令牌的公开接口（登录）不受此分支影响**，输错密码仍是登录页业务失败展示，不会被跳转冲掉提示。`40300` 保留会话，交由页面转入无权限状态。
  - `api/modules/users.ts` + `type.d.ts` 的 `UserSummary`：`GET /users` 契约（字段对齐后端 User 实体对外 JSON，passwordHash 已被服务端 `@JsonIgnore` 排除），`API.d.ts` 统一出口 re-export。
  - `components/ForbiddenState.tsx`：无权限状态卡片（文案「无访问权限 / 当前账号没有管理后台的访问权限」，与后端口径一致，不出现角色、权限码或异常细节）；对无后台权限的账号管理端不存在可安全返回的默认页，唯一恢复出口为退出登录。
  - `hooks/useLogout.ts`：退出登录共用出口（清会话 + 回登录页），`AdminLayout` 侧栏与 `ForbiddenState` 共用。
  - `pages/Users/useUsersService.ts`：页面状态机 `loading | ready | forbidden | error`，进入页面请求 `GET /users`，`40300` → forbidden，其余受控失败透传统一文案；空列表与数据渲染留给 M1-C-06。
- 服务端行为基准（curl 固化）：USER 令牌请求 `GET /api/users` 返回 HTTP 403 + `{"code":40300,"message":"没有访问权限"}`；无效令牌返回 HTTP 401 + `{"code":40100,"message":"请先登录"}`。
- headless Chrome（CDP 驱动真实浏览器，桌面视口 1440×900）7 项检查全部通过：
  - USER（guard03）登录进入 `/users` 展示无权限状态，且令牌保留（不误判为登录失效）。
  - 无权限页文案不含权限码、角色名、令牌片段或异常细节。
  - 无权限页退出登录后回到登录页并清除会话。
  - 无效令牌访问 `/users` 被 401 分支清除会话并整页跳转登录页。
  - 登录页错误密码：停留登录页、展示受控文案「用户名或密码错误」、无整页刷新（页面内存标记存活验证）、不写入令牌。

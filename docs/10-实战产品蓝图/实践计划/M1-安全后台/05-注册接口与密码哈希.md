# M1-05：注册接口与密码哈希

返回 [实践计划与进度](../README.md)。

## 目标

新增用户注册行为。密码只保存 BCrypt 哈希，用户名或邮箱重复时明确拒绝；新用户默认拥有 `USER` 角色。

## 范围

- 为用户模型补充注册所需的用户名、密码哈希和状态字段。
- 新增注册请求 DTO 与注册接口。
- 使用 BCrypt 生成密码哈希。
- 用数据库唯一约束和业务检查共同防止重复注册。
- 注册成功后绑定默认 `USER` 角色。

## 学习点

密码哈希与加盐、唯一约束是最后防线、注册事务边界、默认权限分配。

## 验收

1. 数据库中不出现明文密码。
2. 相同用户名或邮箱再次注册失败，返回可理解错误。
3. 新用户只能获得 `USER`，不能由请求体指定 `ADMIN`。

## 不做

不做邮箱验证码、找回密码、第三方登录、密码复杂度策略和异步通知。

## 完成记录

日期：2026-09-09

提交：`ce73611`（feat: 完成用户注册与密码哈希）

测试与接口证据：

- `cd boot-server && ./mvnw -pl services/user-service -am test`：30 个测试通过（新增 AuthControllerWebTests 7 个 + RegisterFlowIntegrationTests 6 个，并扩展全局异常与真实 HTTP 前缀验证）。
- Web 层覆盖：`POST /api/auth/register` 合法请求返回新用户 ID，真实服务只接受 `/api/auth/register`；请求体携带 `role`/`roles` 被 DTO 白名单丢弃；空用户名、空/非法邮箱、空密码均返回 HTTP 400 / 40000 且不进入 Service；重复用户名返回 HTTP 409 / 40900。
- 服务层集成覆盖：库中 `password_hash` 为 BCrypt 哈希（非明文）且不会被用户查询接口序列化；相同密码两次注册因加盐产生不同哈希；重复用户名/重复邮箱均返回 HTTP 409 / 40900；新用户只绑定 `USER` 角色；M0 旧式创建（不填注册字段）仍可用且 status 取数据库默认值；复跑 `schema.sql` 后注册仍可用。
